# TripEase
TripEase is a backend cab booking system built using Spring Boot and MySQL. It allows customers to book available cabs based on their destination, automatically assigns drivers, manages booking records, and sends email confirmations after successful booking.
